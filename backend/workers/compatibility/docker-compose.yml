services:
  compatibility-worker:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    volumes:
      - ./config.yaml:/app/config.yaml:ro
    network_mode: "host"
    restart: unless-stopped